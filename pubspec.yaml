name: team_flo_app
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.2

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.13.0
  firebase_auth: ^6.5.7
  cloud_firestore: ^6.8.0
  firebase_storage: ^13.4.6
  image_picker: ^1.0.0
  video_player: ^2.8.0
  provider: ^6.0.0
  intl: ^0.20.3
  uuid: ^4.0.0
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  material_color_utilities: ^0.13.0

flutter:
  uses-material-design: true

  assets:
    - lib/assets/images/TeamFlo.png
    - lib/assets/images/belt_ranks/